{
  "name": "workbench-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute workbench --file=./schema.sql --remote",
    "db:init:local": "wrangler d1 execute workbench --file=./schema.sql --local"
  }
}
